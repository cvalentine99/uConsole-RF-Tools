# Hardware Control Center Dependencies
# Optimized for HackerGadgets uConsole AIO Extension Board

# GUI Framework
PyQt5>=5.15.0

# Serial Communication (backup for direct GPS access)
pyserial>=3.5

# GPS via gpsd (recommended - gpsd handles NMEA parsing)
gps3>=0.33.3
gpsd-py3>=0.3.0

# GPS/NMEA Parsing (fallback for direct serial)
pynmea2>=1.18.0

# USB Device Enumeration
pyusb>=1.2.1

# RTL-SDR Support
pyrtlsdr>=0.2.92

# Data Visualization (for spectrum display)
pyqtgraph>=0.12.0
numpy>=1.21.0

# SPI Communication (for LoRa SX1262)
spidev>=3.5

# I2C Communication (for RTC PCF85063A)
smbus2>=0.4.0

# Meshtastic API (optional - for LoRa via Meshtastic)
requests>=2.28.0
